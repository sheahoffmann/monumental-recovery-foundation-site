# Monumental Recovery Foundation — Netlify configuration

[build]
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Never let the function source be served as a static file.
[[redirects]]
  from = "/netlify/*"
  to = "/404.html"
  status = 404
  force = true
